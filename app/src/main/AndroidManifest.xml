<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.kuhrusty.morbadscorepad"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
    <!-- android:supportsRtl="true" -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ChooseCampaignLogActivity">
        </activity>
        <activity android:name=".EditCampaignActivity">
        </activity>
        <activity android:name=".ChooseAdventurerActivity">
        </activity>
        <activity android:name=".EditAdventurerActivity">
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.kuhrusty.morbadscorepad.MainActivity"/>
        </activity>
        <!-- android:label="@string/title_activity_help" -->
        <activity android:name=".HelpActivity">
        </activity>
        <activity android:name=".SkillListActivity">
        </activity>
        <activity android:name=".DangerDeckActivity">
        </activity>
    </application>

</manifest>